# simmconnect-enforcer

This software created force feeback based on data provided SimConnect

# To use

You will need to have python along with the required pip packages installed

```
$ pip install -r requirements.txt
```
